# Reiseplaner (privat genutzt)

Interaktiver Tagesplaner für eine private Reise. Abhaken, Notizen,
Spots zwischen Tagen verschieben — Stand bleibt lokal im Browser.

Generiert aus `gen_planer.py` + `gen_site.py`.
